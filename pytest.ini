[pytest]
testpaths = tests
addopts = -ra -q
markers =
    hosttool: requires an external host tool/checkout (a sibling repo checkout); skips cleanly when absent
    integration: end-to-end / integration arm; may skip when a daemon or built fixture is unavailable
