__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
.peers/
.worktrees/
.handoff-msg.txt
handoff-msg.txt

# Secrets / credentials — never commit. Belt-and-suspenders alongside
# scripts/scrub-guard.py (which scans staged content). Keep key material
# out of `git add` in the first place.
*.key
*.pem
.env
.env.*
*.claude.json
secrets.*
*credentials*
*.secret
